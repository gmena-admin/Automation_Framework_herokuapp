#Missing fields
missingFirstName = firstName: Path `firstName` is required.
missingLastName = lastName: Path `lastName` is required.
missingEmail = email: Email is invalid
missingPassword = password: Path `password` is required.

#Wrong values
wrongEmailFormat = email: Email is invalid
wrongPasswordLength = password: Path `password` (`$1`) is shorter than the minimum allowed length (7).
sameUser = Email address is already in use